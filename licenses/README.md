License evidence is tracked in `THIRD_PARTY_NOTICES.md`, `DEPENDENCY_REGISTER.md`, and the resolved package metadata in `package-lock.json`.

When dependencies are updated, copy any required upstream license text into this directory if the package license terms require bundled notice text beyond package metadata.
